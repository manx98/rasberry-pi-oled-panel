#include "astra_rocket.h"

int main() {
    astraCoreInit();
    astraCoreStart();
//    for(const auto& interface_name: getWifiDevices()) {
//        getWifiList(interface_name);
//        getWifiConnectInfo(interface_name);
//    }
//    spdlog::info("Thermal zone: {}", getThermalZoneTemp());
//    setIpLinkUp("eth01", true);
//    for(auto &item: getNetworkInfos()){
//        spdlog::info("Interface: {}, inet: {}, inet6: {}, up: {}", item.first, item.second.inet, item.second.inet6, item.second.up);
//    }
//    auto mem_info = getMemoryInfo();
//    spdlog::info("Memory info: total: {}, free: {}, buffers: {}, cached: {}, available: {}", mem_info.total, mem_info.free, mem_info.buffers, mem_info.cached, mem_info.available);
}
